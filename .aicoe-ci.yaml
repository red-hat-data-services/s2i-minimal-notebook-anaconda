check:
  - thoth-build
build:
  build-stratergy: Dockerfile
  dockerfile-path: Dockerfile
  registry: quay.io
  registry-org: modh
  registry-project: s2i-minimal-notebook-anaconda
  registry-secret: modh-pusher-secret
  custom-tag: latest
